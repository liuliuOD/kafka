`<TestClassOrComponent>` <briefly describe the problem and its cause>. This
change <briefly describe the fix and any important invariant that remains
unchanged>.

Test time (JUnit XML execution time, excluding Gradle build and compilation):

| Case | Before | After |
| ---- | -----: | ----: |
| `<case>` | `<time>` | `<time>` |
| **Both cases** | `<time>` | `<time>` |

<State whether this is a test-only change and whether production code, public
APIs, or assertions are affected.>

The PR title and body will become the squashed commit message. If you would
like to tag individuals, add commentary, upload images, or include other
supplemental information that should not be part of the eventual commit
message, please use a separate comment.
